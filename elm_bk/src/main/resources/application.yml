server:
  port: 8081

spring:
  application:
    name: elm
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://REDACTED_IP:3306/elm
    username: elm_user
    password: zhijiage123
  data:
    redis:
      host: localhost
      port: 6379
logging:
  level:
    org.springframework.jdbc.support.JdbcTransactionManager: debug

# 自定义配置
jwt:
  secret: Abc123!@#Def456$%^Ghi789&*()Jkl012_-+=Mnop  # JWT密钥
  expiration: 86400000  # 24小时
  token-header: Authorization

# swagger
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui
