# 问题记录

1 路由依赖与web依赖冲突

在网关模块中排除 Web 依赖和WebSocket


2 路由swagger与mvc依赖冲突

路由不需要接口文档，直接删除相关应用

3 路由默认跳转至安全框架默认认证页面

配置user和路由禁用该功能，在路由鉴权，登录下发token仍由user服务来干

4 user服务注册需要调用积分系统创建积分账户

此为服务间调用第一次测试。由于部分接口不需要鉴权，不需要openfeign设置username请求头，同时注册本身涉及user服务，若在create中再调用usercclient获取userId，则拦截器循环知道超时，所以将id作为参数，并将该接口加入排除路径

5 路由规则顺序

user存在 /user/*/status，会导致后续匹配错误，在测试积分系统时发现，通过路由404，直接访问该服务成功