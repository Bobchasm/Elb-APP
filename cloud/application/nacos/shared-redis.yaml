spring:
  data:
    redis:
      host: ${elm.data.redis.host:xxx}
      port: 6379
      password: ${elm.redis.pw:}
      database: ${elm.redis.db}
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms