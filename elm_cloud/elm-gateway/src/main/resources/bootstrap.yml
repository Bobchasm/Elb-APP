spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: REDACTED_IP:8848
      username: nacos
      password: nacos
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-log.yaml
          - data-id: shared-nacos.yaml